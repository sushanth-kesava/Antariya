const SupportService = require('../services/support.service');

exports.createTicket = async (req, res) => { try { const t = await SupportService.createTicket({ ...req.body, createdBy: req.auth.sub }); res.status(201).json({ success: true, data: t }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
exports.getTickets = async (req, res) => { try { const { status, priority, category, assignedTo, search, page, limit } = req.query; const r = await SupportService.getTickets({ status, priority, category, assignedTo, search, page: parseInt(page) || 1, limit: parseInt(limit) || 20 }); res.json({ success: true, data: r }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
exports.getTicketById = async (req, res) => { try { const t = await SupportService.getTicketById(req.params.id); if (!t) return res.status(404).json({ success: false, message: 'Not found' }); res.json({ success: true, data: t }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
exports.updateStatus = async (req, res) => { try { const t = await SupportService.updateStatus({ ticketId: req.params.id, status: req.body.status, userId: req.auth.sub }); res.json({ success: true, data: t }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
exports.assignTicket = async (req, res) => { try { const t = await SupportService.assignTicket({ ticketId: req.params.id, assignedTo: req.body.assignedTo }); res.json({ success: true, data: t }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
exports.addNote = async (req, res) => { try { const t = await SupportService.addNote({ ticketId: req.params.id, ...req.body, userId: req.auth.sub }); res.json({ success: true, data: t }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
exports.getDashboard = async (req, res) => { try { const s = await SupportService.getDashboard(); res.json({ success: true, data: s }); } catch (e) { res.status(500).json({ success: false, message: e.message }); } };
